// Server Actions desactivadas para static export.
// 'use server';
// import { generatePersonalizedMessage, PersonalizedMessageInput } from '@/ai/flows/personalized-donation-messaging';
// const fallbackMessage = "Your contribution can provide vital AI-powered guidance and support for people affected by HIV in Venezuela. Donate today and make a difference!";
// export async function getPersonalizedMessage(input: PersonalizedMessageInput): Promise<string> {
//   try {
//     const result = await generatePersonalizedMessage(input);
//     return result.message || fallbackMessage;
//   } catch (error) {
//     console.error("Error generating personalized message:", error);
//     return fallbackMessage;
//   }
// }
